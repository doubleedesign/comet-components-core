{
    "name": "ContentImageBasic",
    "description": "A basic image within page content",
    "extends": "ContentImageComponent",
    "abstract": false,
    "isInner": false,
    "attributes": {
        "align": {
            "type": "string",
            "description": "Image alignment",
            "supported": [
                "left",
                "center",
                "right",
                "full"
            ]
        },
        "alt": {
            "type": "string",
            "description": "Alternative text",
            "required": true,
            "default": ""
        },
        "aspectRatio": {
            "type": "AspectRatio",
            "description": "Crop image to the given aspect ratio",
            "supported": [
                "4:3",
                "3:4",
                "1:1",
                "16:9",
                "9:16",
                "3:2",
                "2:3",
                "21:9",
                "2.35:1"
            ]
        },
        "caption": {
            "type": "string",
            "description": "Optional image caption (to appear below the image)"
        },
        "classes": {
            "type": "array<string>",
            "description": "Additional CSS classes",
            "default": [
                "image",
                "image--basic"
            ]
        },
        "context": {
            "type": "string",
            "description": "The kebab-case or BEM element chain name of the parent component or variant (if contextually relevant). Note: For components that use the BEM trait, this must be set before init_bem_classes() is called for class naming to work as expected."
        },
        "height": {
            "type": "string",
            "description": "Set a fixed height for the image"
        },
        "href": {
            "type": "string",
            "description": "URL to link to"
        },
        "id": {
            "type": "string",
            "description": "Unique identifier"
        },
        "orientation": {
            "type": "Orientation",
            "description": "Orientation of the component content, if applicable",
            "supported": [
                "horizontal",
                "vertical"
            ],
            "default": "vertical"
        },
        "scale": {
            "type": "string",
            "description": "How to handle how the image fits the available space This is basically here because the WordPress block editor provides this option along with aspect ratio. The ContentImage class provides more advanced cropping and should NOT also have the scale attribute.",
            "supported": [
                "contain",
                "cover"
            ],
            "default": "contain"
        },
        "shortName": {
            "type": "string",
            "description": "The name of the component without any namespacing, prefixes, etc. Used for BEM block/element naming. Derived from the Blade filename if not explicitly set. If explicitly set, handling is component-specific depending on CSS requirements: some will use it in place of generic defaults, others will add a wrapper or adjust BEM classes but otherwise keep the same HTML structure.",
            "required": true,
            "default": "image"
        },
        "src": {
            "type": "string",
            "description": "Image source URL",
            "required": true
        },
        "style": {
            "type": "array",
            "description": "Inline styles"
        },
        "styleName": {
            "type": "string",
            "description": "Visual style variation."
        },
        "tagName": {
            "type": "Tag",
            "description": "The HTML tag to use for this component",
            "supported": [
                "figure",
                "div"
            ],
            "default": "div"
        },
        "testId": {
            "type": "string",
            "description": "Optionally add a data-testid attribute for automated tests"
        },
        "title": {
            "type": "string",
            "description": "Optional image title (appears on hover)"
        },
        "width": {
            "type": "string",
            "description": "Set a fixed width for the image"
        }
    },
    "traits": [
        "LayoutOrientation",
        "BlockElementModifier"
    ]
}