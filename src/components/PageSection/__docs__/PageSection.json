{
    "name": "PageSection",
    "description": "A basic page element to group nested content that can have its semantics, own sizes and alignments within the bounds of this wrapper's size. This component is intended for layout purposes only, not intended to semantically group content. Unlike Container, it does not enforce nested state upon its inner components, and unlike UIComponent and its children it does not have or propagate context.",
    "extends": "Renderable",
    "abstract": false,
    "isInner": false,
    "attributes": {
        "backgroundColor": {
            "type": "ThemeColor|ThemeGradient",
            "description": "The single background colour of the component."
        },
        "classes": {
            "type": "array<string>",
            "description": "Additional CSS classes",
            "default": [
                "page-section"
            ]
        },
        "id": {
            "type": "string",
            "description": "Unique identifier"
        },
        "shortName": {
            "type": "string",
            "description": "The name of the component without any namespacing, prefixes, etc. Used for BEM block/element naming. Derived from the Blade filename if not explicitly set. If explicitly set, handling is component-specific depending on CSS requirements: some will use it in place of generic defaults, others will add a wrapper or adjust BEM classes but otherwise keep the same HTML structure.",
            "required": true,
            "default": "page-section"
        },
        "size": {
            "type": "ContainerSize",
            "description": "Keyword specifying the relative width of the container for the inner content if the component is not nested inside another layout component. Ignored if the component has an isNested attribute set to true, or other logic determines that it is nested.",
            "supported": [
                "fullwidth",
                "wide",
                "narrow",
                "narrower",
                "small",
                "contained"
            ],
            "default": "contained"
        },
        "style": {
            "type": "array",
            "description": "Inline styles"
        },
        "tagName": {
            "type": "Tag",
            "description": "The HTML tag to use for this component",
            "supported": [
                "div",
                "section",
                "article"
            ],
            "default": "div"
        },
        "testId": {
            "type": "string",
            "description": "Optionally add a data-testid attribute for automated tests"
        }
    },
    "innerComponents": {
        "type": "array<Renderable>",
        "description": "Inner components to be rendered within this component",
        "required": true
    },
    "traits": [
        "BackgroundColor",
        "LayoutContainerSize",
        "ShortName"
    ]
}