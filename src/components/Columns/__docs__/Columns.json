{
    "name": "Columns",
    "description": "Organise content visually with a column-based layout.",
    "extends": "LayoutComponent",
    "abstract": false,
    "attributes": {
        "allowStacking": {
            "type": "bool",
            "description": "Option to explicitly specify whether adapt the layout by stacking columns when the viewport or container is narrow. Defaults to true behaviour but does not put the attribute in the HTML unless explicitly set. You generally do not want to set this if your theme will handle it on a case-by-case basis with custom CSS."
        },
        "backgroundColor": {
            "type": "ThemeColor|ThemeGradient",
            "description": "The background colour of the component."
        },
        "classes": {
            "type": "array<string>",
            "description": "Additional CSS classes",
            "default": []
        },
        "columnLayout": {
            "type": "string",
            "description": "How to lay out inner columns when there is fewer than the $qty allowed for. Which values are supported can depend on the number of content columns provided and the viewport size (e.g., expand-middle will only work for odd numbers of inner Columns, and only when there is enough horizontal space for all columns to be on the same row).",
            "required": true,
            "supported": [
                "even",
                "expand-first",
                "expand-last",
                "expand-middle"
            ],
            "default": "even"
        },
        "context": {
            "type": "string",
            "description": "The kebab-case or BEM element chain name of the parent component or variant (if contextually relevant). Note: For components that use the BEM trait, this must be set before init_bem_classes() is called for class naming to work as expected."
        },
        "hAlign": {
            "type": "Alignment",
            "description": "Horizontal alignment, if applicable",
            "supported": [
                "start",
                "end",
                "center",
                "justify",
                "match-parent"
            ],
            "default": "match-parent"
        },
        "id": {
            "type": "string",
            "description": "Unique identifier"
        },
        "qty": {
            "type": "int",
            "description": "The number of layout columns to use; by default automatically determined by the number of inner components. Can be explicitly set when you want to use the columnLayout option (e.g., two columns laid out as 1/3 + 2/3) and/or hAlign option (e.g., 2 one-third columns centered). The maximum supported by the CSS is 6, so if a larger number is provided it will be adjusted.",
            "required": true,
            "supported": [
                "2",
                "3",
                "4",
                "5",
                "6"
            ],
            "default": 2
        },
        "shortName": {
            "type": "string",
            "description": "The name of the component without any namespacing, prefixes, etc. Used for BEM block/element naming. Derived from the Blade filename if not explicitly set. If explicitly set, handling is component-specific depending on CSS requirements: some will use it in place of generic defaults, others will add a wrapper or adjust BEM classes but otherwise keep the same HTML structure.",
            "required": true,
            "default": "columns-wrapper"
        },
        "size": {
            "type": "ContainerSize",
            "description": "Keyword specifying the relative width of the container for the inner content if the component is not nested inside another layout component. Ignored if the component has an isNested attribute set to true, or other logic determines that it is nested.",
            "supported": [
                "fullwidth",
                "wide",
                "narrow",
                "narrower",
                "small",
                "contained"
            ],
            "default": "contained"
        },
        "style": {
            "type": "array",
            "description": "Inline styles"
        },
        "tagName": {
            "type": "Tag",
            "description": "The HTML tag to use for this component",
            "supported": [
                "div",
                "section"
            ],
            "default": "section"
        },
        "testId": {
            "type": "string",
            "description": "Optionally add a data-testid attribute for automated tests"
        },
        "vAlign": {
            "type": "Alignment",
            "description": "Vertical alignment, if applicable",
            "supported": [
                "start",
                "end",
                "center",
                "justify",
                "match-parent"
            ],
            "default": "match-parent"
        }
    },
    "innerComponents": {
        "type": "array<Column>",
        "description": "Inner column components",
        "required": true
    },
    "traits": [
        "BackgroundColor",
        "LayoutAlignment",
        "LayoutContainerSize",
        "BlockElementModifier"
    ]
}